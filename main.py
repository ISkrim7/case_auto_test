import asynciofrom app import hubfrom app.scheduler import Scheduler# @app.on_event("startup")# async def init_aps():#     """#     启动aps 任务#     :return:#     """#     from config import Config#     if Config.APS:#         aps = AsyncIOScheduler()#         await Scheduler.init(aps)#         # todo UI截图删除#         Scheduler.start()@hub.on_event("startup")async def init_db():    """    初始化数据库    :return:    """    from app.model import async_engine,BaseModel    # BaseModel.metadata.create_all(bind=async_engine)    # asyncio.create_task(create_table())    ...@hub.on_event("shutdown")def shutdown_aps():    Scheduler.shutdown()
#!/usr/bin/env python# -*- coding:utf-8 -*-# @Time : 2025/2/18# @Author : cyq# @File : interfaceGlobal# @Software: PyCharm# @Desc:from sqlalchemy import Column, String, ForeignKey, INTEGERfrom app.model import BaseModelclass InterfaceGlobalHeader(BaseModel):    """全局header"""    __tablename__ = 'interface_global_header'    key = Column(String(40), nullable=False, unique=True, comment="key")    value = Column(String(100), nullable=False, comment="value")    project_id = Column(INTEGER, ForeignKey('project.id', ondelete="cascade"),                        nullable=False, comment="所属项目")    def __repr__(self):        return (            f"<InterfaceGlobalHeader(key='{self.key}', value='{self.value}' project={self.project_id})>"        )class InterfaceGlobalVariable(BaseModel):    """全局变量"""    __tablename__ = 'interface_global_variable'    key = Column(String(40), nullable=False, unique=True, comment="key")    value = Column(String(100), nullable=False, comment="value")    project_id = Column(INTEGER, ForeignKey('project.id', ondelete="cascade"),                        nullable=False, comment="所属项目")    def __repr__(self):        return (            f"<InterfaceGlobalVariable(key='{self.key}', value='{self.value}' project={self.project_id})>"        )